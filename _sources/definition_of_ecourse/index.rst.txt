Definition of ecourse
=====================

From this page we can learn how to define the calculates in the course.

We have estudents in a school with averages. Each student is in a course. For each course, we need to calculate the average. Also, we need to calculate the average of all the school.

In order to ilustrate this example, we have the following paramaters and the definitions:

.. image:: ../_static/definition_ecourse.png
